package com.flf.controller.app;

import org.apache.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;

import com.flf.service.ShopIdlePicService;

/**
 * 
 * <br>
 * <b>功能：</b>ShopIdlePicController<br>
 * <br>
 */
@Controller
public class ShopIdlePicAppController {

	private final static Logger log = Logger.getLogger(ShopIdlePicAppController.class);
	@Autowired(required = false)
	private ShopIdlePicService shopIdlePicService;

}
